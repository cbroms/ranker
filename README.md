# Ranker

Ranking stuff.

```
npm install
npm run dev
```

Or you can run the API and client separately:

Run the Sapper project:

```
npm run sapper-dev
```

Run the API server:

```
npm run api-dev
```
